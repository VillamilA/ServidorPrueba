package servidor.servicio;

import servidor.servicio.Servidor;

public class TestServidor {
    public static void main(String[] args) {
        Servidor servidor = new Servidor();
        servidor.servicio(); // Inicia el servidor
    }
}
